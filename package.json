{
  "name": "dominion",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.0.1",
    "@hotwired/turbo-rails": "^7.1.1",
    "@rails/ujs": "^7.0.4",
    "esbuild": "^0.16.17",
    "esbuild-plugin-stimulus": "^0.1.5",
    "sortablejs": "^1.15.0",
    "stimulus-sortable": "^3.2.0"
  },
  "devDependencies": {},
  "scripts": {
    "notbuild": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets",
    "build": "node esbuild.config.js"
  }
}
