= check_box_tag "journal[params][#{control.key}][]", value, control.preselect.call(card), class: control.css_class, form: "control_form_#{control.object_id}", id: sanitize_to_id("journal[params][#{control.key}][#{value}]")
= label_tag "journal[params][#{control.key}][#{value}]", control.text