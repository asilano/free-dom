<% control = peeked_control -%>
<%= PendingAction.find(control[:pa_id]).text -%> (PA <%= control[:pa_id] -%>): <% -%>
<% case control[:type]
   when :button -%>
Choose one from your Looked At cards. Valid choices: <%= opts_for_buttons_from_valid(control, :cards, "Card") %>
<% when :latin_radio -%>
Choose separate actions to apply to each of your Looked At cards. Reply using 'Choose', then space-separated copies of '<Card>=><Option>', where Card is one of <%= control[:cards].each_with_index.map {|valid, ix| ix.to_s if valid}.compact.join(', ') -%> and Option is one of <%= control[:options].each_with_index.map {|opt, ix| "#{ix} for '#{opt}'"}.join(', ') -%>
<% end %>
