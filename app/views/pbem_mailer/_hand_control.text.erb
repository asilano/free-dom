<% control = hand_control -%>
(PA <%= control[:pa_id] -%>) <%= PendingAction.find(control[:pa_id]).text.html_safe -%>: <% -%>
<% case control[:type]
   when :button -%>
Choose one from your hand cards. Valid choices: <%= opts_for_buttons_from_valid(control, :cards, "Card") %>
<% when :checkboxes -%>
Valid responses: '<%= control[:name] -%>' then a Ruby-style array of entries from <%= control[:cards].each_with_index.map {|valid, ix| "#{ix}" if valid}.compact.join(', ') %>
<% end %>
